EventEmitter = require("EventEmitter")

export default DownloadManager extends EventEmitter {
  constructor(queue) {
    super()
    this.queue = queue;
  }

  addAlbum(album) {
  }

  start() {
  }

  stop() {
  }
}
